import 'package:flutter/material.dart';

class AzkarElSalah extends StatelessWidget {
  const AzkarElSalah({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Card(child: Text("Azkar El Salah")),
    );
  }
}
