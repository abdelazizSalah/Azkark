import 'package:flutter/material.dart';

class tasabeeh extends StatelessWidget {
  const tasabeeh({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Card(child: Text("empty")),
    );
  }
}
